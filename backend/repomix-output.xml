This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
apps/
  posts/
    migrations/
      __init__.py
      0001_initial.py
      0002_rename_comment_comment_content_alter_post_image.py
    __init__.py
    admin.py
    apps.py
    helpers.py
    models.py
    serializers.py
    tests.py
    urls.py
    views.py
  user/
    migrations/
      __init__.py
      0001_initial.py
    __init__.py
    admin.py
    apps.py
    authentication.py
    helpers.py
    models.py
    serializers.py
    services.py
    tests.py
    urls.py
    views.py
  __init__.py
config/
  __init__.py
  asgi.py
  paths.py
  settings.py
  urls.py
  wsgi.py
media/
  user_1/
    profile_picture/
      download.jpeg
    test_picture.jpg
  user_16/
    06e2328a-c53a-441a-9ff4-575283e8adca.jpg
    Screenshot_2026-02-18_at_18.09.41.png
  user_28/
    profile_picture/
      download.jpeg
      parrot_picture_kmeans_algorithm.jpg
    download.jpeg
__init__.py
manage.py
requirements.txt
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="apps/posts/migrations/__init__.py">

</file>

<file path="apps/posts/migrations/0001_initial.py">
# Generated by Django 6.0.3 on 2026-04-03 16:49

import django.db.models.deletion
from django.conf import settings
from django.db import migrations, models


class Migration(migrations.Migration):

    initial = True

    dependencies = [
        migrations.swappable_dependency(settings.AUTH_USER_MODEL),
    ]

    operations = [
        migrations.CreateModel(
            name='Post',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('title', models.CharField(max_length=100)),
                ('content', models.TextField()),
                ('image', models.ImageField(blank=True, null=True, upload_to='posts/')),
                ('created_at', models.DateTimeField(auto_now_add=True)),
                ('updated_at', models.DateTimeField(auto_now=True)),
                ('author', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, related_name='posts', to=settings.AUTH_USER_MODEL)),
            ],
        ),
        migrations.CreateModel(
            name='Comment',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('created_at', models.DateTimeField(auto_now_add=True)),
                ('comment', models.TextField()),
                ('author', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, to=settings.AUTH_USER_MODEL)),
                ('post', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, related_name='comments', to='posts.post')),
            ],
        ),
        migrations.CreateModel(
            name='Like',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('created_at', models.DateTimeField(auto_now_add=True)),
                ('author', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, to=settings.AUTH_USER_MODEL)),
                ('post', models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, related_name='likes', to='posts.post')),
            ],
            options={
                'ordering': ['-created_at'],
                'unique_together': {('author', 'post')},
            },
        ),
    ]
</file>

<file path="apps/posts/migrations/0002_rename_comment_comment_content_alter_post_image.py">
# Generated by Django 6.0.3 on 2026-04-29 12:02

import apps.posts.helpers
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('posts', '0001_initial'),
    ]

    operations = [
        migrations.RenameField(
            model_name='comment',
            old_name='comment',
            new_name='content',
        ),
        migrations.AlterField(
            model_name='post',
            name='image',
            field=models.ImageField(blank=True, null=True, upload_to=apps.posts.helpers.user_directory_path),
        ),
    ]
</file>

<file path="apps/posts/__init__.py">

</file>

<file path="apps/posts/admin.py">
from django.contrib import admin

# Register your models here.
</file>

<file path="apps/posts/apps.py">
from django.apps import AppConfig


class PostsConfig(AppConfig):
    name = 'apps.posts'
</file>

<file path="apps/posts/helpers.py">
def user_directory_path(instance, filename):
    return f'user_{instance.author.id}/post_content/{filename}'
</file>

<file path="apps/posts/models.py">
from django.db import models
from config import settings
from apps.posts.helpers import user_directory_path

# Create your models here.

class Post(models.Model):
    # Link to the user, the one who created the post
    author = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.CASCADE,
        related_name='posts'
    )

    # Content
    title = models.CharField(max_length=100)
    content = models.TextField()
    image = models.ImageField(upload_to=user_directory_path, null=True, blank=True)#upload to already has instance and filename


    # Metadata for bot detection
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    def __str__(self):
        return f"{self.author.username}-{self.title}"


class Like(models.Model):
    #the one who created the like
    author = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.CASCADE,
    )
    post = models.ForeignKey(Post, on_delete=models.CASCADE, related_name='likes')
    created_at = models.DateTimeField(auto_now_add=True)

    class Meta:
        unique_together = ('author', 'post')
        ordering = ['-created_at'] #shows the list of likes in descending order





class Comment(models.Model):
    #the one who commented
    author = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.CASCADE,
    )
    post = models.ForeignKey(Post, on_delete=models.CASCADE, related_name='comments')
    created_at = models.DateTimeField(auto_now_add=True)
    content = models.TextField()

    """
    to understan more clearly, here i can acces both comments from the author and vice versa
    
    example:
    post.comments.all() gets all the comments from one post
    
    comment.post gets tge post of that comment, there is only one anyway
    
    """
</file>

<file path="apps/posts/serializers.py">
from datetime import timedelta
from django.utils import timezone

from apps.posts.models import Post, Comment, Like
from rest_framework import serializers


class PostSerializer(serializers.ModelSerializer):
    likes_count = serializers.SerializerMethodField()
    comments_count = serializers.SerializerMethodField()
    is_liked = serializers.SerializerMethodField()
    class Meta:
        model = Post
        fields = ['id','author','title','content','created_at','image','likes_count','comments_count','is_liked']

    author = serializers.ReadOnlyField(source='author.username')
    """on this one i have to write a little because im just starting to understand how this works
    the thin is i need to validate that a person cannot make more than 20 posts in one day, for bot spamming reasons
    so i have to get the user who s logged in, i have to request the data
    then i have to define the beggining of the dau 
    
    """
    def validate(self, data):
        # get the data from the authenticated user
        user = self.context['request'].user
        # set a variable with the beggining of the day
        today_start = timezone.now().replace(hour=0, minute=0, second=0, microsecond=0)

        # count how many posts have the timestamp greater or equal with the beggining of the day, if thee are more than 20 then raise error
        #objects is used to use methods for the models
        count = Post.objects.filter(author=user, created_at__gte = today_start).count()

        if count >= 20:
            raise serializers.ValidationError('You cannot post more than 20 posts in one day')
        #i wanted to put here validated data but this happens before is validated, im just learning now its not my fault
        return data

    def get_likes_count(self, post):
        return post.likes.count()
    def get_comments_count(self, post):
        return post.comments.count()

    def get_is_liked(self, obj):
        # Grab the user making the request from the context
        request = self.context.get('request')

        # If they are logged in, check if a Like exists for them on this post
        if request and request.user.is_authenticated:
            return Like.objects.filter(post=obj, author=request.user).exists()

        return False


class CommentSerializer(serializers.ModelSerializer):
    author = serializers.ReadOnlyField(source='author.username')
    class Meta:
        model = Comment
        fields = ['id', 'author','content', 'created_at']

    def validate(self, data):
        user = self.context['request'].user

        # set a variable for the time of the comment an hour ago
        hour_beginning = timezone.now() - timedelta(hours=1)

        #count all the cooments left by an user that happened after an hour ago, if there are more than 20 raise error
        count = Comment.objects.filter(author=user, created_at__gte = hour_beginning).count()

        if count >= 20:
            raise serializers.ValidationError('You cannot post more than 20 comments in one day')
        return data


class LikeSerializer(serializers.ModelSerializer):
    author = serializers.ReadOnlyField(source='author.username')
    class Meta:
        model = Like
        fields = ['id','author', 'post_id','created_at']

        read_only_fields = ['post_id']

    def validate(self, data):
        user = self.context['request'].user

        # set a variable for the time of the comment an hour ago
        hour_beginning = timezone.now() - timedelta(hours=1)

        # count all the likes left by an user that happened after an hour ago, if there are more than 20 raise error
        count = Like.objects.filter(author=user, created_at__gte=hour_beginning).count()

        if count >= 300:
            raise serializers.ValidationError('Take a break bro')
        return data
</file>

<file path="apps/posts/tests.py">
from django.test import TestCase

# Create your tests here.
</file>

<file path="apps/posts/urls.py">
from django.urls import path,include


from rest_framework.routers import DefaultRouter

from apps.posts import views
from apps.posts.views import CommentViewSet, LikeViewSet, getMyPosts
from apps.user.views import getMyProfile
router = DefaultRouter()
router.register(r'posts', views.PostViewSet)


urlpatterns = [
    path('',include(router.urls)),

    path('profile/', getMyProfile, name='my-profile'),
    path('profile/posts/', getMyPosts, name='my-posts'),

    path('posts/<int:post_pk>/comments/',CommentViewSet.as_view(
        {'get':'list','post':'create','put':'update','patch':'partial_update','delete':'destroy'}),
    name='comment-list'),

    path('posts/<int:post_pk>/likes/',LikeViewSet.as_view(
        {'get':'list','post':'create','put':'update','patch':'partial_update','delete':'destroy'}),
          name='like-list'
         )

]
</file>

<file path="apps/posts/views.py">
from django.shortcuts import get_object_or_404
from rest_framework import viewsets, permissions
from .models import Post, Comment, Like
from .serializers import PostSerializer, CommentSerializer, LikeSerializer
from rest_framework import status
from rest_framework.decorators import api_view, permission_classes
from rest_framework.response import Response


# Create your views here.
class IsAuthorOrReadOnly(permissions.BasePermission):
    def has_object_permission(self, request, view, obj):
        if request.method in permissions.SAFE_METHODS: # GET, HEAD, OPTIONS
            return True
        return obj.author == request.user # Only the author can PUT or DELETe



class PostViewSet(viewsets.ModelViewSet):
    queryset = Post.objects.all().order_by('-created_at')
    serializer_class = PostSerializer

    permission_classes = (permissions.IsAuthenticated, IsAuthorOrReadOnly)

    def perform_create(self, serializer):
        serializer.save(author=self.request.user)

class CommentViewSet(viewsets.ModelViewSet):

    serializer_class = CommentSerializer

    permission_classes = (permissions.IsAuthenticated, IsAuthorOrReadOnly)

    def perform_create(self, serializer):
        post_id = self.kwargs.get('post_pk')
        post_instance = get_object_or_404(Post, id=post_id)
        serializer.save(author=self.request.user, post=post_instance)# this takes the user from the token


    def get_queryset(self):
        queryset = Comment.objects.all()

        post_id = self.kwargs.get('post_pk')
        return Comment.objects.filter(post_id=post_id)


class LikeViewSet(viewsets.ModelViewSet):
    serializer_class = LikeSerializer
    permission_classes = (permissions.IsAuthenticated, IsAuthorOrReadOnly)

    def perform_create(self, serializer):
        post_id = self.kwargs.get('post_pk')

        # 2. Fetch the actual Post object from the database
        post_instance = get_object_or_404(Post, id=post_id)

        # 3. Save the Like with BOTH the author AND the post attached!
        # 👇 If "post=post_instance" is missing here, you get that IntegrityError! 👇
        serializer.save(author=self.request.user, post=post_instance)
    def get_queryset(self):
        queryset = Like.objects.all()

        post_id = self.kwargs.get('post_pk')
        return Like.objects.filter(post_id=post_id)

    def destroy(self, request, *args, **kwargs):

        post_id = self.kwargs.get('post_pk')

        like_to_delete = Like.objects.filter(post_id=post_id,author=self.request.user)
        if like_to_delete.exists():
            like_to_delete.delete()
            return Response(status=status.HTTP_204_NO_CONTENT)
        else:
            return Response({"detail": "You haven't liked this post."}, status=status.HTTP_404_NOT_FOUND)


@api_view(['GET'])
@permission_classes([permissions.IsAuthenticated])
def getMyPosts(request):
    # Filter posts to only include those created by the logged-in user
    user_posts = Post.objects.filter(author=request.user).order_by('-created_at')

    # We pass the request in the context so the Serializer can calculate 'is_liked'
    serializer = PostSerializer(user_posts, many=True, context={'request': request})
    return Response(serializer.data)
</file>

<file path="apps/user/migrations/__init__.py">

</file>

<file path="apps/user/migrations/0001_initial.py">
# Generated by Django 6.0.3 on 2026-03-24 13:31

import django.contrib.auth.models
import django.contrib.auth.validators
import django.db.models.deletion
import django.utils.timezone
from django.conf import settings
from django.db import migrations, models


class Migration(migrations.Migration):

    initial = True

    dependencies = [
        ('auth', '0012_alter_user_first_name_max_length'),
    ]

    operations = [
        migrations.CreateModel(
            name='User',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('password', models.CharField(max_length=128, verbose_name='password')),
                ('last_login', models.DateTimeField(blank=True, null=True, verbose_name='last login')),
                ('is_superuser', models.BooleanField(default=False, help_text='Designates that this user has all permissions without explicitly assigning them.', verbose_name='superuser status')),
                ('username', models.CharField(error_messages={'unique': 'A user with that username already exists.'}, help_text='Required. 150 characters or fewer. Letters, digits and @/./+/-/_ only.', max_length=150, unique=True, validators=[django.contrib.auth.validators.UnicodeUsernameValidator()], verbose_name='username')),
                ('first_name', models.CharField(blank=True, max_length=150, verbose_name='first name')),
                ('last_name', models.CharField(blank=True, max_length=150, verbose_name='last name')),
                ('is_staff', models.BooleanField(default=False, help_text='Designates whether the user can log into this admin site.', verbose_name='staff status')),
                ('is_active', models.BooleanField(default=True, help_text='Designates whether this user should be treated as active. Unselect this instead of deleting accounts.', verbose_name='active')),
                ('date_joined', models.DateTimeField(default=django.utils.timezone.now, verbose_name='date joined')),
                ('email', models.EmailField(max_length=254, unique=True)),
                ('created_at', models.DateTimeField(auto_now_add=True)),
                ('updated_at', models.DateTimeField(auto_now=True)),
                ('groups', models.ManyToManyField(blank=True, help_text='The groups this user belongs to. A user will get all permissions granted to each of their groups.', related_name='user_set', related_query_name='user', to='auth.group', verbose_name='groups')),
                ('user_permissions', models.ManyToManyField(blank=True, help_text='Specific permissions for this user.', related_name='user_set', related_query_name='user', to='auth.permission', verbose_name='user permissions')),
            ],
            options={
                'verbose_name': 'user',
                'verbose_name_plural': 'users',
                'abstract': False,
            },
            managers=[
                ('objects', django.contrib.auth.models.UserManager()),
            ],
        ),
        migrations.CreateModel(
            name='Profile',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('profile_pic', models.ImageField(blank=True, null=True, upload_to='profile_pic')),
                ('bio', models.TextField(blank=True, null=True)),
                ('website', models.URLField(blank=True, null=True)),
                ('user', models.OneToOneField(on_delete=django.db.models.deletion.CASCADE, to=settings.AUTH_USER_MODEL)),
            ],
        ),
    ]
</file>

<file path="apps/user/__init__.py">

</file>

<file path="apps/user/admin.py">
from django.contrib import admin
from .models import *

# Register your models here.

admin.site.register(User)
admin.site.register(Profile)
</file>

<file path="apps/user/apps.py">
from django.apps import AppConfig


class AccountsConfig(AppConfig):
    name = 'apps.user'
</file>

<file path="apps/user/authentication.py">
from rest_framework_simplejwt.authentication import JWTAuthentication

class CustomCookieAuthentication(JWTAuthentication):
    def authenticate(self, request):
        raw_token = request.COOKIES.get('access_token')
        if not raw_token:
            return None
        validated_token = self.get_validated_token(raw_token)
        logged_user = self.get_user(validated_token)
        response = ( logged_user,validated_token)
        return response
</file>

<file path="apps/user/helpers.py">
def user_directory_path(instance, filename):
    return f'user_{instance.user.id}/profile_picture/{filename}'
</file>

<file path="apps/user/models.py">
from django.db import models #the database methods
from django.contrib.auth.models import AbstractUser #the security methods
from apps.user.helpers import user_directory_path


# Create your models here.


class User(AbstractUser):
    email = models.EmailField(unique=True) #sets email to unique
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)



class Profile(models.Model):
    user = models.OneToOneField(User, on_delete=models.CASCADE)     #links the user and the profile
    profile_pic = models.ImageField(upload_to=user_directory_path, null=True, blank=True)     #the photo stored locally, null for database and blank for frontend(allows them to be empty)
    bio = models.TextField(null=True, blank=True)               #same here
    website = models.URLField(null=True, blank=True)


    def __str__(self):
        return self.user.username
</file>

<file path="apps/user/serializers.py">
from rest_framework import serializers
from .models import User, Profile

# 👇 1. INCOMING DATA (Validation Only)
class UserRegistrationSerializer(serializers.Serializer):
    username = serializers.CharField(max_length=150)
    email = serializers.EmailField()
    password = serializers.CharField()

class UserLoginSerializer(serializers.Serializer):
    username = serializers.CharField()
    password = serializers.CharField()

class UpdateProfileSerializer(serializers.Serializer):
    first_name = serializers.CharField(max_length=150, required=False, allow_blank=True)
    last_name = serializers.CharField(max_length=150, required=False, allow_blank=True)
    bio = serializers.CharField(required=False, allow_blank=True)
    website = serializers.URLField(required=False, allow_blank=True)
    profile_pic = serializers.ImageField(required=False, allow_null=True)


class UpdateSecuritySerializer(serializers.Serializer):
    username = serializers.CharField(max_length=150, required=False)
    email = serializers.EmailField(required=False)
    password = serializers.CharField(required=False)

class PasswordResetRequestSerializer(serializers.Serializer):
    email = serializers.EmailField()

class PasswordResetConfirmSerializer(serializers.Serializer):
    new_password = serializers.CharField()


# 👇 2. OUTGOING DATA (Formatting Only)
class ProfileResponseSerializer(serializers.ModelSerializer):
    username = serializers.ReadOnlyField(source='user.username')
    first_name = serializers.ReadOnlyField(source='user.first_name')
    last_name = serializers.ReadOnlyField(source='user.last_name')
    email = serializers.ReadOnlyField(source='user.email')

    class Meta:
        model = Profile
        fields = ['username', 'first_name', 'last_name', 'email', 'bio', 'profile_pic', 'website']
        read_only_fields = fields
</file>

<file path="apps/user/services.py">
from django.contrib.auth import authenticate
from rest_framework_simplejwt.tokens import RefreshToken
from django.core.mail import send_mail
from django.conf import settings
from django.contrib.auth.tokens import default_token_generator
from django.utils.http import urlsafe_base64_encode, urlsafe_base64_decode
from django.utils.encoding import force_bytes, force_str

from .models import User, Profile


class ProfileService:
    """Handles ONLY public-facing profile logic."""

    @staticmethod
    def create_profile(user: User) -> Profile:
        profile, _ = Profile.objects.get_or_create(user=user) # tries to get the object, if not then it creates it
        return profile

    @staticmethod
    def update_profile(user: User, bio: str = None, website: str = None,profile_pic: str = None) -> Profile:
        profile = ProfileService.create_profile(user)

        if bio is not None:
            profile.bio = bio
        if website is not None:
            profile.website = website

        if profile_pic is not None:
            profile.profile_pic = profile_pic

        profile.save()
        return profile


class UserService:
    """Handles ONLY security, identity, and authentication logic."""

    @staticmethod
    def register_user(username: str, email: str, password: str) -> User:
        if User.objects.filter(username=username).exists():
            raise ValueError("This username is already taken.")
        if User.objects.filter(email=email).exists():
            raise ValueError("This email is already registered.")
        if len(password) < 8 or not any(char.isdigit() for char in password):
            raise ValueError("Password must be at least 8 characters long.")

        user = User.objects.create_user(username=username, email=email, password=password, is_active=False)
        return user

    @staticmethod
    def update_credentials(user: User, username: str = None, email: str = None):
        """Handles sensitive unique identifiers (Username & Email)."""

        # Check if they are trying to change the username, and if it's available
        if username and username != user.username:
            if User.objects.filter(username=username).exists():
                raise ValueError("This username is already taken.")
            user.username = username

        # Check if they are trying to change the email, and if it's available
        if email and email != user.email:
            if User.objects.filter(email=email).exists():
                raise ValueError("This email is already registered.")
            user.email = email

        # If anything changed, save it
        if username or email:
            user.save()

    # ... keep your update_password method right below this!


class AuthService:
    FRONTEND_URL = "http://localhost:5173"

    @staticmethod
    def login(username: str, password: str) -> dict:
        user = authenticate(username=username, password=password)
        if not user:
            raise ValueError("Invalid username or password.")

        refresh = RefreshToken.for_user(user)
        return {
            "username": user.username,
            "access_token": str(refresh.access_token),
            "refresh_token": str(refresh),
            "is_active": str(user.is_active)
        }

    @staticmethod
    def send_activation_email(user: User):
        uid = urlsafe_base64_encode(force_bytes(user.pk))
        token = default_token_generator.make_token(user)
        link = f"{AuthService.FRONTEND_URL}/activate/{uid}/{token}"

        send_mail(
            "Confirm your DOT8 Account",
            f"Hi {user.username},\nClick here to activate: {link}",
            settings.DEFAULT_FROM_EMAIL,
            [user.email],
        )

    @staticmethod
    def verify_email(uidb64: str, token: str):
        try:
            uid = force_str(urlsafe_base64_decode(uidb64))
            user = User.objects.get(pk=uid)
        except (TypeError, ValueError, OverflowError, User.DoesNotExist):
            raise ValueError("Invalid activation link.")

        if not default_token_generator.check_token(user, token):
            raise ValueError("Activation link is expired or invalid.")

        user.is_active = True
        user.save()

    @staticmethod
    def request_password_reset(email: str):
        try:
            user = User.objects.get(email=email)
            uid = urlsafe_base64_encode(force_bytes(user.pk))
            token = default_token_generator.make_token(user)
            link = f"{AuthService.FRONTEND_URL}/reset-password/{uid}/{token}"

            send_mail(
                "Reset your DOT8 Password",
                f"Click the link to reset your password:\n{link}",
                settings.DEFAULT_FROM_EMAIL,
                [user.email],
            )
        except User.DoesNotExist:
            pass # Silent fail for security

    @staticmethod
    def confirm_password_reset(uidb64: str, token: str, new_password: str):
        try:
            uid = force_str(urlsafe_base64_decode(uidb64))
            user = User.objects.get(pk=uid)
        except (TypeError, ValueError, OverflowError, User.DoesNotExist):
            raise ValueError("Invalid reset link.")

        if not default_token_generator.check_token(user, token):
            raise ValueError("Reset link is expired or invalid.")

        if len(new_password) < 8 or not any(char.isdigit() for char in new_password):
            raise ValueError("Password must be at least 8 characters long and contain a number.")

        user.set_password(new_password)
        user.save()
</file>

<file path="apps/user/tests.py">
from django.test import TestCase

# Create your tests here.
</file>

<file path="apps/user/urls.py">
from django.urls import path
from apps.user  import views



urlpatterns = [
    path('all', views.getAllUsers, name='all'),
    path('signUp', views.singUpUser, name='singUp'),
    path('signIn',views.signInUser, name='signIn'),
    path('signIn/refresh',views.signInUser,name='signIn'),
    path('logout', views.logOutUser, name='logout'),
    path('profile/update/', views.updateMyProfile, name='update-profile'),
    path('security/update/', views.updateMySecurity, name='update-security'),
    path('activate/<str:uidb64>/<str:token>/', views.verifyEmail, name='activate'),
    path('password-reset/request/', views.requestPasswordReset, name='password-reset-request'),
    path('password-reset/confirm/<str:uidb64>/<str:token>/', views.confirmPasswordReset, name='password-reset-confirm'),




]
</file>

<file path="apps/user/views.py">
from rest_framework import status
from rest_framework.decorators import api_view, permission_classes
from rest_framework.permissions import IsAuthenticated
from rest_framework.response import Response

from .serializers import (
    UserRegistrationSerializer,
    UserLoginSerializer,
    UpdateProfileSerializer,
    UpdateSecuritySerializer,
    PasswordResetRequestSerializer,
    PasswordResetConfirmSerializer,
    ProfileResponseSerializer
)
from .services import UserService, AuthService, ProfileService
from .models import Profile


@api_view(['POST'])
def singUpUser(request):
    input_serializer = UserRegistrationSerializer(data=request.data)
    if not input_serializer.is_valid():
        return Response(input_serializer.errors, status=status.HTTP_400_BAD_REQUEST)

    try:
        user = UserService.register_user(
            username=input_serializer.validated_data['username'],
            email=input_serializer.validated_data['email'],
            password=input_serializer.validated_data['password']
        )
        AuthService.send_activation_email(user)
        return Response({"message": "Please check your email to activate your account."},
                        status=status.HTTP_201_CREATED)
    except ValueError as e:
        return Response({"error": str(e)}, status=status.HTTP_400_BAD_REQUEST)


@api_view(['POST'])
def signInUser(request):
    input_serializer = UserLoginSerializer(data=request.data)
    if not input_serializer.is_valid():
        return Response(input_serializer.errors, status=status.HTTP_400_BAD_REQUEST)

    try:
        auth_data = AuthService.login(
            username=input_serializer.validated_data['username'],
            password=input_serializer.validated_data['password']
        )

        response = Response({"status": "success", "username": auth_data["username"]}, status=status.HTTP_200_OK)
        response.set_cookie('access_token', value=auth_data["access_token"], httponly=True)
        response.set_cookie('refresh_token', value=auth_data["refresh_token"], httponly=True)
        return response
    except ValueError as e:
        return Response({"error": str(e)}, status=status.HTTP_401_UNAUTHORIZED)


@api_view(['POST'])
def logOutUser(request):
    response = Response({"status": "success", "message": "Logged out"}, status=status.HTTP_200_OK)
    response.delete_cookie('access_token')
    response.delete_cookie('refresh_token')
    return response


@api_view(['POST'])
def verifyEmail(request, uidb64, token):
    try:
        AuthService.verify_email(uidb64, token)
        return Response({"message": "Account activated successfully!"}, status=status.HTTP_200_OK)
    except ValueError as e:
        return Response({"error": str(e)}, status=status.HTTP_400_BAD_REQUEST)


@api_view(['POST'])
def requestPasswordReset(request):
    input_serializer = PasswordResetRequestSerializer(data=request.data)
    if not input_serializer.is_valid():
        return Response(input_serializer.errors, status=status.HTTP_400_BAD_REQUEST)

    AuthService.request_password_reset(input_serializer.validated_data['email'])
    return Response({"message": "If an account exists, a reset link has been sent."}, status=status.HTTP_200_OK)


@api_view(['POST'])
def confirmPasswordReset(request, uidb64, token):
    input_serializer = PasswordResetConfirmSerializer(data=request.data)
    if not input_serializer.is_valid():
        return Response(input_serializer.errors, status=status.HTTP_400_BAD_REQUEST)

    try:
        AuthService.confirm_password_reset(
            uidb64=uidb64,
            token=token,
            new_password=input_serializer.validated_data['new_password']
        )
        return Response({"message": "Password reset successfully. You can now log in."}, status=status.HTTP_200_OK)
    except ValueError as e:
        return Response({"error": str(e)}, status=status.HTTP_400_BAD_REQUEST)


@api_view(['GET'])
@permission_classes([IsAuthenticated])
def getMyProfile(request):
    profile, _ = Profile.objects.get_or_create(user=request.user)
    output_serializer = ProfileResponseSerializer(profile)
    return Response(output_serializer.data)


@api_view(['PATCH'])
@permission_classes([IsAuthenticated])
def updateMyProfile(request):
    input_serializer = UpdateProfileSerializer(data=request.data)
    if not input_serializer.is_valid():
        return Response(input_serializer.errors, status=status.HTTP_400_BAD_REQUEST)

    try:
        profile = ProfileService.update_profile(request.user, input_serializer.validated_data)
        output_serializer = ProfileResponseSerializer(profile)
        return Response(output_serializer.data, status=status.HTTP_200_OK)
    except Exception as e:
        return Response({"error": str(e)}, status=status.HTTP_500_INTERNAL_SERVER_ERROR)


@api_view(['PATCH'])
@permission_classes([IsAuthenticated])
def updateMySecurity(request):
    input_serializer = UpdateSecuritySerializer(data=request.data)
    if not input_serializer.is_valid():
        return Response(input_serializer.errors, status=status.HTTP_400_BAD_REQUEST)

    try:
        UserService.update_credentials(request.user, input_serializer.validated_data)
        return Response({"message": "Security info updated successfully."}, status=status.HTTP_200_OK)
    except ValueError as e:
        return Response({"error": str(e)}, status=status.HTTP_400_BAD_REQUEST)
</file>

<file path="apps/__init__.py">

</file>

<file path="config/__init__.py">

</file>

<file path="config/asgi.py">
"""
ASGI config for config project.

It exposes the ASGI callable as a module-level variable named ``application``.

For more information on this file, see
https://docs.djangoproject.com/en/6.0/howto/deployment/asgi/
"""

import os

from django.core.asgi import get_asgi_application

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'config.settings')

application = get_asgi_application()
</file>

<file path="config/paths.py">

</file>

<file path="config/settings.py">
"""
Django settings for config project.

Generated by 'django-admin startproject' using Django 6.0.3.

For more information on this file, see
https://docs.djangoproject.com/en/6.0/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/6.0/ref/settings/
"""
import os
from pathlib import Path
import environ



# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent
env = environ.Env()
# Read the .env file
environ.Env.read_env(os.path.join(Path(__file__).resolve().parent.parent, '.env'))

# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/6.0/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = env('SECRET_KEY')

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = env.bool('DEBUG', default=False)

ALLOWED_HOSTS = []


# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'rest_framework',
    'apps.user', # this is mine, the first app
    'corsheaders',
    'apps.posts',

]

REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': (
        'apps.user.authentication.CustomCookieAuthentication',

    ),
    'DEFAULT_PERMISSION_CLASSES': (
        'rest_framework.permissions.AllowAny',
    )
}

MIDDLEWARE = [
    'corsheaders.middleware.CorsMiddleware',
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',

]

ROOT_URLCONF = 'config.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'config.wsgi.application'


# Database
# https://docs.djangoproject.com/en/6.0/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': env('DB_NAME'),
        'USER': env('DB_USERNAME'),  # Replace with your Mac login name
        'PASSWORD': env('DB_PASSWORD'),              # Usually blank by default on Mac
        'HOST': env('DB_HOST'),
        'PORT': env('DB_PORT'),
    }
}


# Password validation
# https://docs.djangoproject.com/en/6.0/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/6.0/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/6.0/howto/static-files/

STATIC_URL = 'static/'

AUTH_USER_MODEL = 'user.User'

MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')

CORS_ALLOW_CREDENTIALS = True

CORS_ALLOWED_ORIGINS = [
    "http://localhost:5173",
    "http://127.0.0.1:5173",
]

CSRF_TRUSTED_ORIGINS = [
    "http://localhost:5173",
]

from datetime import timedelta

SIMPLE_JWT = {
    # How long the "Working" token lasts (e.g., for Axios calls)
    'ACCESS_TOKEN_LIFETIME': timedelta(days=1),

    # How long before the user is FORCED to log in again
    'REFRESH_TOKEN_LIFETIME': timedelta(days=30),

    'ROTATE_REFRESH_TOKENS': True,
    'BLACKLIST_AFTER_ROTATION': True,
}

EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'
DEFAULT_FROM_EMAIL = 'noreply@dot8.com'
</file>

<file path="config/urls.py">
"""
URL configuration for config project.

The `urlpatterns` list routes URLs to views. For more information please see:
    https://docs.djangoproject.com/en/6.0/topics/http/urls/
Examples:
Function views
    1. Add an import:  from my_app import views
    2. Add a URL to urlpatterns:  path('', views.home, name='home')
Class-based views
    1. Add an import:  from other_app.views import Home
    2. Add a URL to urlpatterns:  path('', Home.as_view(), name='home')
Including another URLconf
    1. Import the include() function: from django.urls import include, path
    2. Add a URL to urlpatterns:  path('blog/', include('blog.urls'))
"""
from django.conf.urls.static import static
from django.contrib import admin
from django.urls import path, include
from django.urls import path, include
from rest_framework_simplejwt import views as jwt_views

from config import settings

urlpatterns = [
    path('admin/', admin.site.urls),
    path('api/v1/', include('apps.user.urls')),
    path('api/v1/account/',include('apps.posts.urls')),
]


if settings.DEBUG:
    urlpatterns += static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)
</file>

<file path="config/wsgi.py">
"""
WSGI config for config project.

It exposes the WSGI callable as a module-level variable named ``application``.

For more information on this file, see
https://docs.djangoproject.com/en/6.0/howto/deployment/wsgi/
"""

import os

from django.core.wsgi import get_wsgi_application

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'config.settings')

application = get_wsgi_application()
</file>

<file path="__init__.py">

</file>

<file path="manage.py">
#!/usr/bin/env python
"""Django's command-line utility for administrative tasks."""
import os
import sys


def main():
    """Run administrative tasks."""
    os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'config.settings')
    try:
        from django.core.management import execute_from_command_line
    except ImportError as exc:
        raise ImportError(
            "Couldn't import Django. Are you sure it's installed and "
            "available on your PYTHONPATH environment variable? Did you "
            "forget to activate a virtual environment?"
        ) from exc
    execute_from_command_line(sys.argv)


if __name__ == '__main__':
    main()
</file>

<file path="requirements.txt">
asgiref==3.11.1
Django==6.0.3
django-environ==0.13.0
psycopg2-binary==2.9.11
sqlparse==0.5.5
</file>

</files>
